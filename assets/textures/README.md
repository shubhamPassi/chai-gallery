# Texture assets

The current chalk grain is generated in CSS to keep the page lightweight. Add an optimized texture file here if you want to replace it later, then update the `body::before` background in `css/style.css`.
